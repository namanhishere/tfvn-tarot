"""Artifact catalog and hash-integrity checks (todo A.2)."""

from __future__ import annotations

from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[3]

# Placeholder — A.2 owns the full catalog registry, row counts, sha256 digests
# and the canonical-serialisation hash checks vs kb/CARDS_HASH.txt and
# datasets/DATASET_HASH.txt.

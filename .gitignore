# --- W0.2: vendor data is a one-off clone, never tracked (490 MB corpus) ---
data/*
!data/MANIFEST.md
!data/CORPUS_HASH.txt

# --- environments & caches ---
.venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

# --- secrets: template is .env.example (tracked), real values never committed ---
.env
.env.*
!.env.example

# --- generation prompt-hash cache (W2.2 / W3) ---
.cache/

# --- model artifacts ---
*.gguf
artifacts/

# --- os / editor ---
.DS_Store
*.swp
.idea/
.vscode/

# --- runtime / session state (not pipeline artifacts) ---
.omo/
.codegraph

# --- W3 raw generated output (bulk, regenerable) ---
datasets/raw/

# --- runtime logs (regenerable) ---
logs/

# --- W3.3 raw base-model baseline corpus (regenerable; summary .json is tracked) ---
datasets/base_diversity_baseline.jsonl
